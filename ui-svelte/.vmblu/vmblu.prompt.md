# vmblu Project

The model file set lives in `model/`.
Node implementations live in `nodes/`.
This project is based on the svelte framework. Svelte components that are not standalone but part of a node live in /fragments.

This project is not a runnable app, but a library of nodes.

General vmblu instructions come from the installed vmblu skill/plugin/CLI docs.