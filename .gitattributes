###############################################################################
# Line endings (cross-platform consistency)
###############################################################################
# Keep LF in the repo; let editors handle local display
* text=auto eol=lf

# Scripts that must keep specific endings
*.sh    text eol=lf
*.bash  text eol=lf
*.bat   text eol=crlf
*.cmd   text eol=crlf

# Lock files (avoid accidental CRLF)
package-lock.json text eol=lf
yarn.lock         text eol=lf
pnpm-lock.yaml    text eol=lf

###############################################################################
# Treat binary assets as binary (no diffs/merges)
###############################################################################
# Images / textures
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.svg  binary
*.ico  binary
*.bmp  binary
*.webp binary
*.avif binary
*.exr  binary
*.hdr  binary

# 3D / data assets
*.glb  binary
*.gltf binary
*.bin  binary

# Audio / video
*.mp3  binary
*.wav  binary
*.ogg  binary
*.mp4  binary
*.webm binary

# Fonts
*.ttf  binary
*.otf  binary
*.woff binary
*.woff2 binary

# WASM and packaged artifacts
*.wasm binary
*.zip  binary
*.7z   binary
*.tar  binary
*.gz   binary
*.vsix binary

###############################################################################
# Optional: Git LFS (uncomment if you decide to use LFS)
###############################################################################
# Filter large assets via LFS instead of normal Git objects
# *.exr  filter=lfs diff=lfs merge=lfs -text
# *.hdr  filter=lfs diff=lfs merge=lfs -text
# *.glb  filter=lfs diff=lfs merge=lfs -text
# *.mp4  filter=lfs diff=lfs merge=lfs -text
# *.webm filter=lfs diff=lfs merge=lfs -text
