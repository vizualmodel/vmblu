# sysblu vscode editor

## Node

Hosts the reusable sysblu application in a VS Code webview. The shell owns only VS Code communication and UI composition; the linked sysblu application owns the editable system document and its history.

## Pins
